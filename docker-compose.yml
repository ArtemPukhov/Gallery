services:
  app:
    build:
      context: .
      args:
        FETCH_ARTISTS_SKIP: "1"
    environment:
      NODE_ENV: production
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
